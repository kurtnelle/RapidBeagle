# RapidBeagle external tree Kconfig hooks.
# Each custom package gets a `source` line below.

menu "RapidBeagle custom packages"

source "$BR2_EXTERNAL_RAPIDBEAGLE_PATH/package/rtl8812au/Config.in"

endmenu
